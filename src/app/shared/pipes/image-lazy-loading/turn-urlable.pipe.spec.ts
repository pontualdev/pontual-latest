import { TurnUrlablePipe } from './turn-urlable.pipe';

describe('TurnUrlablePipe', () => {
  it('create an instance', () => {
    const pipe = new TurnUrlablePipe();
    expect(pipe).toBeTruthy();
  });
});
